{% comment %} Generic Page {% endcomment %}
<div style="padding-top:120px; min-height:80vh;">
  <div class="section" style="max-width:800px;">
    <h1 class="section-title">{{ page.title }}</h1>
    <div style="font-size:0.95rem; color:var(--text-mid); line-height:1.9; margin-top:2rem;">
      {{ page.content }}
    </div>
  </div>
</div>
